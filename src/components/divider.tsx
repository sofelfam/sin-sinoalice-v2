import React from 'react';
import 'twin.macro';

const Component: React.FCX = ({ className }) => (
  <hr className={className} tw='border-none h-px m-0 flex-shrink-0 bg-gray-300' />
);

export default Component;
